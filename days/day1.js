// 1) Program to print 1 to num, num to 1, -1 to -num, -num to -1 by using while loop

    let i = 1;
    while (i <= 10) {
        console.log(i + " ");
        i++;
    }
    // console.log("code is error");

    var j = 10;
    while (!(j == 0)) {
        console.log(j );
        j--;
    }
    // console.log("code is error");

    let k = -1;
    while (!(k === -10-1)) {
        console.log(k);
        k--;
    }
    // console.log("\n");

    let l = -10;
    while (!(l === 0)) {
        console.log(l );
        l++;
    }
    // console.log("erroor");

    
    // 2) Write a program to find the largest prime in the given number
    // input: 46537
    // output: 7
    

    // let num1 = 46537;
    // let largestPrime = 1;
    
    // for (let z = 2; z <= num1; z++) {
    //     while (num1 % z === 0) {
    //         largestPrime = z;
    //         num1 = num1 / z;
    //     }
    // }
    
    // console.log("Largest prime factor of 46537 is:", largestPrime);
    